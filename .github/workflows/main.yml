name: CI/CD MLflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CSV_URL: "MLProject/Forest cover_preprocessing_dataset.csv"
  TARGET_VAR: "Cover_Type"

jobs:
  train-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v3

      - name: 🐍 Setup Python 3.12.7
        uses: actions/setup-python@v4
        with:
          python-version: "3.12.7"

      - name: 📥 Setup Conda environment
        uses: conda-incubator/setup-miniconda@v2
        with:
          environment-file: MLProject/conda.yaml
          activate-environment: Forest_Cover_Classification
          auto-activate-base: false

      - name: 🧠 Install dependencies
        run: |
          conda init bash
          source ~/.bashrc
          conda activate Forest_Cover_Classification
          pip install mlflow
          touch MLProject/requirements.txt  # optional, jika file kosong
          pip install -r MLProject/requirements.txt

      - name: 🚀 Run MLflow Project
        run: |
          source ~/.bashrc
          conda activate Forest_Cover_Classification
          export MLFLOW_TRACKING_URI=./MLProject/mlruns
          mlflow run MLProject --env-manager=local

      - name: 🔍 Get Latest MLflow RUN_ID
        id: get_run_id
        run: |
          source ~/.bashrc
          conda activate Forest_Cover_Classification
          cd MLProject
          echo "🔍 Checking mlruns/0/"
          ls -al mlruns/0 || echo "⚠️ mlruns/0 not found or empty"

          RUN_ID=$(ls -td mlruns/0/*/ 2>/dev/null | head -n 1 | awk -F'/' '{print $3}')
          if [ -z "$RUN_ID" ]; then
            echo "❌ RUN_ID not found. Make sure MLflow run was successful."
            exit 1
          fi
          echo "✅ Found RUN_ID=$RUN_ID"
          echo "run_id=$RUN_ID" >> $GITHUB_OUTPUT

      - name: 📂 Show logged artifacts
        run: |
          source ~/.bashrc
          conda activate Forest_Cover_Classification
          cd MLProject
          RUN_ID=${{ steps.get_run_id.outputs.run_id }}
          echo "Artifacts for run_id: $RUN_ID"
          ls -R mlruns/0/$RUN_ID/artifacts/

      - name: ✅ Verify model artifacts exist
        run: |
          source ~/.bashrc
          conda activate Forest_Cover_Classification
          cd MLProject
          RUN_ID=${{ steps.get_run_id.outputs.run_id }}
          ARTIFACT_DIR="mlruns/0/$RUN_ID/artifacts"
          if [ ! -d "$ARTIFACT_DIR/best_model_tuned" ]; then
            echo "❌ best_model_tuned not found in $ARTIFACT_DIR"
            exit 1
          fi
          echo "✅ Model artifacts verified."

      - name: 📤 Commit mlruns to repository
        run: |
          git config --global user.name "${{ secrets.GIT_USERNAME }}"
          git config --global user.email "${{ secrets.GIT_EMAIL }}"
          git remote set-url origin https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }}.git
          git add MLProject/mlruns || true
          git commit -m "Add mlruns from CI run" || echo "No changes to commit"
          git push origin main
        env:
          GH_PAT: ${{ secrets.GH_PAT }}

      - name: 🐳 Build Docker image from MLflow model
        run: |
          source ~/.bashrc
          conda activate Forest_Cover_Classification
          cd MLProject
          RUN_ID=${{ steps.get_run_id.outputs.run_id }}
          mlflow models build-docker \
            --model-uri "file://mlruns/0/$RUN_ID/artifacts/best_model_tuned" \
            --name cc

      - name: 🔐 Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: 🏷️ Tag Docker image
        run: |
          docker tag cc ${{ secrets.DOCKER_HUB_USERNAME }}/${{ secrets.DOCKER_HUB_REPO }}:latest

      - name: 📦 Push Docker image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_HUB_USERNAME }}/${{ secrets.DOCKER_HUB_REPO }}:latest

